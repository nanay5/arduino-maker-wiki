---
title: "HC-SR04 超声波避障小车"
slug: "students/ultrasonic"
section: "students"
category: "进阶项目"
kit: "ai-smart-car"
parts: ["hc-sr04", "l298n", "tt-motor", "arduino-uno"]
difficulty: 3
duration: "90分钟"
version: "1.0"
updated: "2026-06-09"
---

import VersionBadge from '../../../components/VersionBadge.astro';
import DifficultyStars from '../../../components/DifficultyStars.astro';
import KitCTA from '../../../components/KitCTA.astro';
import Troubleshooting from '../../../components/Troubleshooting.astro';

<VersionBadge version={frontmatter.version} updated={frontmatter.updated} />
&nbsp;&nbsp;
<DifficultyStars difficulty={frontmatter.difficulty} />
&nbsp;&nbsp;
<span style="font-size:0.85rem;color:var(--color-text-secondary);">⏱️ {frontmatter.duration}</span>

---

## 📋 材料清单

| 元件 | 型号 | 数量 |
|---|---|---|
| 主控板 | Arduino Uno R3 | 1 |
| 超声波传感器 | HC-SR04 | 1 |
| 电机驱动板 | L298N | 1 |
| TT 马达 + 轮子 | — | 2 |
| 小车底盘 | — | 1 |
| 电池盒（6-12V） | — | 1 |
| 杜邦线 | 公母各一包 | 若干 |

## 🔌 接线图

> *此处放置接线图*

| HC-SR04 | Arduino |
|---|---|
| VCC | 5V |
| GND | GND |
| Trig | D9 |
| Echo | D10 |

| L298N | Arduino |
|---|---|
| IN1 | D5 |
| IN2 | D6 |
| IN3 | D7 |
| IN4 | D8 |
| ENA | D3 |
| ENB | D11 |
| 12V | 外接电源正极 |
| GND | 外接电源负极 + Arduino GND |

## 💻 代码

```cpp
#define TRIG_PIN 9
#define ECHO_PIN 10

#define IN1 5
#define IN2 6
#define IN3 7
#define IN4 8
#define ENA 3
#define ENB 11

void setup() {
  pinMode(TRIG_PIN, OUTPUT);
  pinMode(ECHO_PIN, INPUT);
  pinMode(IN1, OUTPUT); pinMode(IN2, OUTPUT);
  pinMode(IN3, OUTPUT); pinMode(IN4, OUTPUT);
  pinMode(ENA, OUTPUT); pinMode(ENB, OUTPUT);
  Serial.begin(9600);
}

float getDistance() {
  digitalWrite(TRIG_PIN, LOW);
  delayMicroseconds(2);
  digitalWrite(TRIG_PIN, HIGH);
  delayMicroseconds(10);
  digitalWrite(TRIG_PIN, LOW);
  long duration = pulseIn(ECHO_PIN, HIGH);
  return duration * 0.034 / 2;
}

void forward() {
  digitalWrite(IN1, HIGH); digitalWrite(IN2, LOW);
  digitalWrite(IN3, HIGH); digitalWrite(IN4, LOW);
  analogWrite(ENA, 150); analogWrite(ENB, 150);
}

void turnLeft() {
  digitalWrite(IN1, LOW); digitalWrite(IN2, HIGH);
  digitalWrite(IN3, HIGH); digitalWrite(IN4, LOW);
  analogWrite(ENA, 120); analogWrite(ENB, 120);
}

void loop() {
  float dist = getDistance();
  Serial.print("距离: "); Serial.print(dist); Serial.println(" cm");
  if (dist < 20) { turnLeft(); delay(500); }
  else { forward(); }
  delay(100);
}
```

## ⚡ 上传与测试

1. 接好所有线，确保 L298N 已接外接电源
2. 上传代码，打开串口监视器（波特率 9600）
3. 小车放在地面，用手挡在前方，观察是否自动转向

<div class="success-box">
  <h4>✅ 成功现象</h4>
  <p>小车正前方无障碍时直行；障碍物距离小于 20cm 时自动左转避障。串口监视器持续打印距离值（cm）。</p>
</div>

<div class="challenge-box">
  <h4>🏆 挑战任务</h4>
  <ul>
    <li><strong>基础：</strong>实现前方避障（同本教程）</li>
    <li><strong>进阶：</strong>同时检测左右两侧距离，选择更空旷的方向转弯</li>
    <li><strong>高手：</strong>让小车在简单迷宫中走出最短路径</li>
  </ul>
</div>

<Troubleshooting parts={frontmatter.parts} section={frontmatter.section} />

<KitCTA kit={frontmatter.kit} section={frontmatter.section} />
